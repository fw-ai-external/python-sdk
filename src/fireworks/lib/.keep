This directory holds hand-maintained code that extends the SDK.

The REST API client code generator does not write into this directory; its contents (other than this keep file) are preserved across regenerations.